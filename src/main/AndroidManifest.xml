<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amazingtech.petrolfinder">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/fb_app_id"></meta-data>

        <activity android:name=".Activity_CityChoice" />
        <activity android:name=".Activity_AddPost" />
        <activity android:name=".ActivityMenuLayout" />
        <activity android:name=".Activity_Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_Signup" />
        <activity android:name=".ActivitySearch" />
        <activity android:name=".Activity_MyAdd" />
        <activity android:name=".Activity_Updatepost" />
        <activity android:name=".ActivityAboutUs"></activity>
    </application>

</manifest>